# 项目摘要（用于新会话继承记忆）

更新时间：2026-03-22

## 1. 当前阶段结论

- 项目已完成 Phase 1 的核心收口：文档、配置说明、基础测试、启动自检已落地。
- Web 端已具备最小可用闭环（身份 + 会话 + 历史 + 聊天）。
- 已完成一轮“仅改边界”的会话策略最小落地：仅改 `transport`、`skill`、`bootstrap`、`main`。

## 2. 本轮已完成的关键改动

### 2.1 高价值回归测试

- 新增 [`tests/test_command_dispatcher.py`](../tests/test_command_dispatcher.py)
- 新增 [`tests/test_reminder_service.py`](../tests/test_reminder_service.py)
- 新增 [`tests/test_web_api_regression.py`](../tests/test_web_api_regression.py)
- 已有 [`tests/test_event_handler.py`](../tests/test_event_handler.py)
- 已有 [`tests/test_smoke.py`](../tests/test_smoke.py)

### 2.2 Web MVP 稳定化与会话策略（边界内改动）

- `transport` 抽象新增：
  - `resolve_session_scope(context)`
  - `startup_self_check()`
  - 文件：[`src/luoying_bot/ports/transport.py`](../src/luoying_bot/ports/transport.py)
- QQ transport 实现会话作用域 + 启动自检：
  - [`src/luoying_bot/infra/transports/qq_ws_transport.py`](../src/luoying_bot/infra/transports/qq_ws_transport.py)
- Web transport 实现会话作用域 + 启动自检：
  - [`src/luoying_bot/infra/transports/web_noop_transport.py`](../src/luoying_bot/infra/transports/web_noop_transport.py)
- 新增会话策略技能：
  - [`src/luoying_bot/application/agent/skills/session_policy_skill.py`](../src/luoying_bot/application/agent/skills/session_policy_skill.py)
- Web/QQ 容器注入 `session_policy`，Web 注入 `web_session_store`：
  - [`src/luoying_bot/bootstrap_web.py`](../src/luoying_bot/bootstrap_web.py)
  - [`src/luoying_bot/bootstrap.py`](../src/luoying_bot/bootstrap.py)
- 启动日志新增 transport 自检与策略输出：
  - [`src/luoying_bot/main_web.py`](../src/luoying_bot/main_web.py)
  - [`src/luoying_bot/main_qq.py`](../src/luoying_bot/main_qq.py)

### 2.3 运维与回归脚本

- Ollama 状态检查脚本（模型列表为空判错）：
  - [`tests/check_ollama_status.py`](../tests/check_ollama_status.py)
- Web MVP 回归脚本（Ubuntu 可用）：
  - [`tests/web_mvp_regression.sh`](../tests/web_mvp_regression.sh)

## 3. 文档同步状态

- 已同步更新：
  - [`README.md`](../README.md)
  - [`documents/部署文档.md`](./部署文档.md)
  - [`documents/配置说明.md`](./配置说明.md)
  - [`documents/项目当前进度与后续分步路线图.md`](./项目当前进度与后续分步路线图.md)

## 4. 最近一次合并后验收结果

已执行并通过：

- `python -m unittest discover -s tests -v`（28 tests 全通过）
- `cd src && python -m luoying_bot.tools.selfcheck`（PASS）
- `python tests/check_ollama_status.py`（PASS，模型可用）
- `TestClient` 下 Web 基础链路：创建会话 + 正常 `/api/chat` 可返回 200

本轮已完成的关键修复：

- `/api/chat` 跨用户 session 异常分支已统一转成结构化 HTTP JSON 错误响应（`detail.code/message/status`）
- `/chat` 与 `/api/chat` 的 400/502/500 错误契约已统一
- Web 前端已兼容结构化 `detail`，避免错误提示出现 `[object Object]`
- 位置：
  - [`src/luoying_bot/infra/web/api.py`](../src/luoying_bot/infra/web/api.py)
  - [`src/luoying_bot/infra/web/index.html`](../src/luoying_bot/infra/web/index.html)

## 5. 下一阶段建议起手顺序

1. 继续扩大 Web API 错误分支回归覆盖（404/503 与历史写入失败场景）。
2. 推进“会话模型细化 + Web MVP 稳定化二阶段”：
   - 会话清理/归档策略
   - Web API 错误码与返回模型规范
   - LLM 失败分级兜底
3. 补 `ScriptWorkspaceService` 的高价值回归（路线图第一优先级）。

## 6. 常用命令（新会话可直接执行）

```bash
# 全量单测
python -m unittest discover -s tests -v

# 启动自检
cd src
python -m luoying_bot.tools.selfcheck

# 启动 Web
cd src
uvicorn luoying_bot.main_web:create_app --factory --host 0.0.0.0 --port 8000

# Web MVP 回归（需先启动 Web）
./tests/web_mvp_regression.sh

# Ollama 状态检查
python tests/check_ollama_status.py
```
